// The daily plan: a short, finite set of blocks for today, built from the
// curriculum catalog and the evidence engine. Framework-free and deterministic
// per learner and local date, so it works offline and matches across devices.

export * from "./dailyPlan";
export * from "./normalize";
